// Frontend configuration. Endpoints are same-origin (proxied through
// CloudFront), so the browser only needs the public Stripe key.
window.STRIPE_PUBLISHABLE_KEY = "pk_test_00000000000000000000000000";
